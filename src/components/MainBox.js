import React from 'react'

function MainBox({ color }) {
  return <div
      style={{
        backgroundColor: color,
        width: '500px',
        height: '500px'
      }}
    />
}

export default MainBox