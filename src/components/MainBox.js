import React from 'react'

function MainBox({ color }) {
  return <div
      style={{
        backgroundColor: 'blue',
        width: '500px',
        height: '500px'
      }}
    />
}

export default MainBox